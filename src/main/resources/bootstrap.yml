spring:
  application:
    name: currency-exchange-service
  cloud:
    config:
      uri:
      - http://localhost:8888      
  jpa:
    show-sql: true    
  h2:
    console:
      enabled: true

server: 
 port: 8001


